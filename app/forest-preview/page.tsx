import { ForestScene } from "./forest-scene";

// Concept prototype for the web personal forest (M0/M1 vision doc §20.2):
// a horizontally explorable 2.5D pixel forest built from the existing sprite
// assets, with fake data and simulated desktop link. No backend, no auth.

export default function ForestPreviewPage() {
  return <ForestScene />;
}
